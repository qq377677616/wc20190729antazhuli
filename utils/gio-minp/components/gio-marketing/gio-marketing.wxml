<!-- 这是自定义组件的内部WXML结构 -->
<view wx:if="{{display}}" class="inner" catchtouchmove="handleTouchMove">
  <image class="image" bindtap="handleConfirm" src="{{params.src}}"></image>
  <view class="close" bindtap="handleClose">╳</view>
</view>
